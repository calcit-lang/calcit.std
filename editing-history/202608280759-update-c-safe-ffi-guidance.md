# Update C-safe FFI guidance / 更新 C-safe FFI 指南

## 中文

- 移除迁移期保留 legacy Rust ABI fallback 的过时维护说明。
- 明确 calcit.std 动态边界只使用版本化 C-safe protocols。

## English

- Remove stale migration guidance that retained the legacy Rust ABI fallback.
- State that the calcit.std dynamic boundary uses versioned C-safe protocols exclusively.
