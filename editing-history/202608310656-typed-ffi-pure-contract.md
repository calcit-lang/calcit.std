# Typed pure FFI contract / 类型化纯函数 FFI 契约

- Upgrade the project toolchain declaration to Calcit 0.13.69.
- Declare `calcit.std.hash/md5` as the sync-pure typed FFI pilot over
  edn-buffer-v1.
- Document deterministic `calcit ffi export` auditing and keep this baseline
  separate from resource and async lifecycle concerns.

- 将项目工具链声明升级到 Calcit 0.13.69。
- 将 `calcit.std.hash/md5` 声明为基于 edn-buffer-v1 的同步纯函数 typed FFI
  试点。
- 记录确定性 `calcit ffi export` 审计，并让该基准与 resource、async 生命周期
  关注点保持分离。
