# 发布 0.2.25 / Release 0.2.25

- process stream、Ctrl-C handler、interval 和 timeout 现在返回类型化 `FfiTask`。
- 示例统一使用 `.cancel-with`，原始 lifecycle 过程仅留在内部边界。
- 使用 Calcit 0.13.59 完成类型、文档、C-safe ABI 和真实异步回归。

- Process streams, Ctrl-C handlers, intervals, and timeouts now return typed `FfiTask` values.
- Examples consistently use `.cancel-with`, leaving raw lifecycle procedures at the internal boundary.
- Validated types, docs, the C-safe ABI, and real async behavior with Calcit 0.13.59.
