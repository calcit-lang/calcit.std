# Release 0.2.20

- Publish C-safe async protocol v1 adapters for timers, process output, and Ctrl+C subscriptions.
- Expose cancellable `AnyRef` task capabilities and tighter callback/Option schemas.
- Include Linux symbol, runnable docs/examples, and real host cancellation gates in CI.
